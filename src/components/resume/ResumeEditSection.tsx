import React from 'react'

const ResumeEditSection = ({ resume_id }: { resume_id: string }) => {
    return (
        <section>
            ResumeEditSection {resume_id}
        </section>
    )
}

export default ResumeEditSection